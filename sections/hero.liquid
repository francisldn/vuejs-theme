<div class="hero-image section">
  <img src="{{ section.settings.image | img_url: 'master' }}">
</div>

{% comment %} JSON as the schema {% endcomment %}
{% schema %}
{
  "name": "Hero Image", 
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "presets": [
    {
      "name": "Hero Image",
      "category": "Image"
    }
  ]
}
{% endschema %}